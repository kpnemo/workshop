# Test Report — new-workshop
**Generated:** 2026-04-20 11:49  
**Reviewers:** qa-1 (agent-service), qa-2 (web-client + cross-package)

---

## 1. Executive Summary

| Metric | Value |
|---|---|
| Test files | 25 total (20 `agent-service`, 5 `web-client`) |
| agent-service source coverage | 17 / 18 files (94%) |
| web-client source coverage | 4 / 29 files (14%) |
| High-severity quality issues | 12 |
| Medium-severity quality issues | 10 |
| Low-severity quality issues | 5 |
| Untested modules (agent-service) | 1 (`index.ts`) |
| Untested modules (web-client) | 25 (3 hooks, 1 context, 21 components) |

**Biggest risks:**

- [ ] `AuthContext.tsx` has zero tests — the JWT expiry branch (`payload.exp * 1000 > Date.now()`) and token-storage helpers are security-critical and completely uncovered; a one-character regression silently logs all users out.
- [ ] `copilot-route.test.ts` only exercises validation; the entire SSE streaming happy path (`POST /copilot/chat`) is untested — the route's primary logic has no safety net.
- [ ] `routes/conversations.ts` PATCH and POST `/summary` endpoints have zero tests — two user-visible features can regress silently.
- [ ] `auth.test.ts` is missing wrong-secret and malformed-payload JWT cases — an auth-bypass surface is untested in production middleware.
- [ ] `guardrails.ts` prompt-injection via `userMessage` (unescaped template embedding) is untested — a user message containing `"` or `\n` can alter the classification instruction.

---

## 2. Quality Issues

### High Severity

- [ ] Strengthen `deleteAgent` error assertion — `agent-loader.test.ts:229` — `.toThrow()` without a message pattern passes for any error type, masking future regressions on error identity.
- [ ] Add PUT range-validation tests for temperature and maxTokens — `agents-routes.test.ts:64-83` — PUT has weaker coverage than POST; out-of-range values go untested, allowing silent acceptance.
- [ ] Add `/agents/tools` route ordering test — `routes/agents.ts:30-33` — breaking the `/tools`-before-`/:id` ordering would silently 404 all tool requests; no test would catch it.
- [ ] Rename or replace vacuous "does not generate a title while conversation is still on router" test — `auto-mode.test.ts:223-251` — the in-test comment admits it only asserts "no error"; the title-generation path actually runs and the test name is misleading.
- [ ] Add wrong-secret JWT test to auth middleware — `middleware/auth.ts:14` — a syntactically valid JWT signed with the wrong key is not tested; this is a distinct security surface from "malformed token."
- [ ] Add malformed-payload JWT test (missing `userId` field) — `middleware/auth.ts:15` — token whose payload decodes but lacks `userId` sets `req.userId = undefined`; no middleware rejection is tested, exposing a potential auth-bypass path.
- [ ] Add prompt-injection test for `userMessage` in guardrails — `services/guardrails.ts:23-29` — a message containing `"` or `\n` is embedded into the classification template string unescaped; zero coverage on this attack surface.
- [ ] Add PDF read-path tests to file-service — `services/file-service.ts:99-118` — the PDF branch in both `readFileContent` and `generateDescription` has zero coverage; this is a user-visible feature.
- [ ] Add path-traversal filename test — `services/file-service.ts:29` — `path.basename` is the only guard against `../../etc/passwd` filenames; the security behavior is unasserted.
- [ ] Add silent-no-op tests or harden DB update methods — `services/database.ts:173-191, 293-295` — `setTitle`, `setAgentId`, `setActiveAgent`, `setSummary`, `updateFileDescription` all return `void` with no error on unknown id; a systemic correctness risk across the DB layer.
- [ ] Add migration-from-old-schema test — `services/database.ts:216-250` — the `PRAGMA table_info` + `ALTER TABLE` migration branches are never exercised because every test starts from a fresh DB; a broken migration would only surface in production upgrades.
- [ ] Add happy-path SSE streaming tests to copilot-route — `routes/copilot.ts:43-125` — entire streaming path (delta, agent_created, agent_updated, error events; extractAgentConfig; saveAgent; refreshAgents side-effect) is untested.

### Medium Severity

- [ ] Fix `describe("loadAgents")` block containing `deleteAgent` tests — `agent-loader.test.ts:228` — block name no longer reflects scope; split into `describe("saveAgent")` and `describe("deleteAgent")` to improve navigability.
- [ ] Replace hand-rolled `http.request` helper with supertest — `agents-routes.test.ts:22-45` — duplicated across 6+ test files; inconsistency increases maintenance burden.
- [ ] Tighten SSE assertions from `toContain` string-match to parsed-object assertions — `assign-agent.test.ts:36-40`, `delegation.test.ts:95-98`, `file-tools.test.ts:62-68, 111-117` — string-contain passes for malformed JSON and wrong event structure.
- [ ] Add specific error-message assertions replacing generic `toContain("Error")` — `assign-agent.test.ts:67-68`, `update-summary.test.ts:40`, `browse-url.test.ts`, `hand-back.test.ts:17-22` — ordering bugs and wrong-branch behavior would pass current assertions.
- [ ] Switch `Date.now()` DB paths to `mkdtempSync` across all test files — `auth.test.ts:91-101`, `database.test.ts:12-13`, `file-service.test.ts:26-28` — two tests in the same millisecond share a DB under parallel execution.
- [ ] Fix `update-summary` tool returning `success: true` when DB write affects 0 rows — `services/tools/update-summary.ts:32` — `setSummary` on a non-existent conversation silently succeeds; add a test documenting (or fixing) this behavior.
- [ ] Add test asserting `execute("assign_agent")` is gated by calling agent identity, not just `getToolsForAgent` listing — `services/tool-service.ts:84-93` — the authorization gate is only on definition-listing; execute-path is unguarded and untested.
- [ ] Fix `copilot-api.test.ts` reader construction to match `api.test.ts` pattern — `copilot-api.test.ts:12-21` — the `{ getReader: () => stream.getReader() }` wrapper means SSE lines are never actually walked; four of five event handlers are masked.
- [ ] Cover `AuthContext` JWT decode, expiry comparison, and token-persistence — `contexts/AuthContext.tsx` — the `exp * 1000 > Date.now()` branch is the most security-visible line in the frontend with zero tests.
- [ ] Add `fetchAvailableTools` silent-fallback assertion — `agents-api.ts:12` — the intentional `return []` on non-2xx should be explicitly documented in a test so a future "throw instead" change is caught.

### Low Severity

- [ ] Use `expect.arrayContaining` instead of exact-order assertion in privileges test — `privileges.test.ts:11-17` — catalog ordering is presentational; a display-order change breaks the test for a non-behavioral reason.
- [ ] Extract shared `testHelpers/http.ts` with consolidated `makeRequest`/`mockFetch` setup — `api.test.ts`, `agents-api.test.ts`, `copilot-api.test.ts` — duplicated boilerplate across three frontend test files.
- [ ] Replace mutable top-level mock functions with per-test `vi.mocked()` — `auto-mode.test.ts:31-44`, `routes.test.ts:45-57` — state-leaky mock reassignment causes ordering-dependent failures under `--threads`.
- [ ] Add `expect(onTitle).not.toHaveBeenCalled()` and equivalent for all uncalled SSE callback spies — `api.test.ts:80`, `use-chat.test.ts` — uncalled callbacks are the leading regression source for SSE parsers.
- [ ] Remove dead `vi.mock("playwright")` declaration with no exercising test — `tool-service.test.ts:4-15` — defensive mock is unexplained; add a comment or add a test that makes it load-bearing.

---

## 3. Coverage Gaps

### agent-service (`packages/agent-service`)

#### `services/agent-loader.ts`
- [ ] Add test: one bad file does not abort loading of remaining files — `agent-loader.ts:24-25` — file-read error is silently skipped; no test asserts partial-failure tolerance.
- [ ] Add test: round-trip for agent with non-empty `tools` array — `agent-loader.ts:70-72` — `saveAgent` omits `tools` when the array is empty but the populated-array path is untested.
- [ ] Add test: combined "missing name and model" validation — `agent-loader.ts:27-29` — only individual-field-missing cases are tested.

#### `routes/agents.ts`
- [ ] Add test: `GET /agents/tools` route exists and is ordered before `/:id` — `routes/agents.ts:30-33` — routing order bug would silently 404; no test locks the ordering.
- [ ] Add tests: `maxTokens` range validation (`< 1`, `> 4096`) on POST — `routes/agents.ts:48` — upper/lower bounds are unchecked.
- [ ] Add tests: PUT with out-of-range temperature, out-of-range maxTokens, clearing optional fields — `routes/agents.ts:64-83` — PUT validation coverage is weaker than POST on every field.
- [ ] Add tests: edge-case slug behavior (all-whitespace name, punctuation-only, unicode, PUT that changes slug) — `routes/agents.ts:7-9` — empty-slug agents can be registered at `""` silently.

#### `services/tools/assign-agent.ts`
- [ ] Add test: `undefined` context passed to tool — `assign-agent.ts:28` — `!context` branch is unreachable via current tests.
- [ ] Add test: "once per conversation" contract — double-assign within same conversation — `assign-agent.ts:43-49` — the behavioral contract is documented but neither enforced nor tested.

#### `middleware/auth.ts` + `routes/auth.ts`
- [ ] Add test: `Authorization: "Basic xxx"` header rejected — `middleware/auth.ts:9` — only "no header" is covered; wrong-scheme is untested.
- [ ] Add test: login with empty-string email and password — `routes/auth.ts:16-17` — only missing-field cases are tested on login path.
- [ ] Add test: login/signup with non-string password types (null, {}, []) — `routes/auth.ts:17`, `13-18` — `bcrypt.compare` on non-string throws with an unclear error.

#### `routes/conversations.ts`
- [ ] Add tests: `POST /conversations/:id/summary` endpoint — `routes/conversations.ts:492-539` — 404 cross-user, empty-messages early return, 500 on Haiku failure, successful `setSummary` call are all untested.
- [ ] Add tests: `PATCH /conversations/:id` endpoint — `routes/conversations.ts:542-561` — `summaryEnabled` toggle, non-boolean no-op, 404 cross-user are all untested.
- [ ] Add test: active agent deleted between request start and loop iteration — `routes/conversations.ts:116-126`, `175-179` — `!curAgent` branch inside delegation loop is never reached.
- [ ] Add test: delegation-aware message construction — delegate and main-agent message filtering, `delegation_end` → synthetic user message — `routes/conversations.ts:188-213`.
- [ ] Add test: `MAX_TOOL_ITERATIONS = 5` cap reached — `routes/conversations.ts:261` — infinite-tool-use exit condition has zero coverage.
- [ ] Add test: multiple `tool_use` blocks in a single LLM response — `routes/conversations.ts:347-402` — only single-tool scenarios exist.
- [ ] Add test: `update_summary` tool emits `summary` SSE event — `routes/conversations.ts:387-395`.
- [ ] Add test: title generation throws (catch path) — `routes/conversations.ts:476-478`.

#### `services/guardrails.ts`
- [ ] Add test: `response.content[0].type !== "text"` (e.g. tool_use response) — `guardrails.ts:37-39` — falls through to `allowed: true`; untested.

#### `services/database.ts`
- [ ] Add test: `setTitle` on non-existent conversation id — `database.ts:187-191` — silent no-op; 0 rows affected is indistinguishable from success.
- [ ] Add test: `setAgentId` on non-existent conversation id — `database.ts:173-177`.
- [ ] Add test: `listConversations` sort order (`updated_at DESC`) — `database.ts:129-137`.
- [ ] Add test: `getGroup` for unknown group id — `database.ts:302-332`.
- [ ] Add test: migration from pre-existing older schema (ALTERs applied correctly) — `database.ts:216-250`.

#### `services/file-service.ts`
- [ ] Add test: large-file truncation appends `[Content truncated]` marker — `file-service.ts:121-125`.
- [ ] Add test: Haiku returns zero text blocks → fallback to "No description available." — `file-service.ts:138-139`.
- [ ] Add test: `readFileContent` with wrong userId returns "not found" — `file-service.ts:94-96`.

#### `routes/files.ts`
- [ ] Add test: file named without extension (`noext`) is rejected — `routes/files.ts:40` — `.undefined` extension rejection is accidental, not by design.
- [ ] Add test: uppercase extension (`.PDF`) is rejected — `routes/files.ts:40` — real user-visible bug; `.PDF` would be rejected because the allowlist is lowercase-only.
- [ ] Add test: `GET` file cross-user isolation — `routes/files.ts` — auth isolation only covers DELETE; GET is untested for cross-user access.

#### `services/tools/delegate-to.ts` + `hand-back.ts`
- [ ] Add test: `mainAgentId` fallback `"unknown"` branch when no agent in map has this delegate — `delegate-to.ts:30`.
- [ ] Add test: `hand_back` when `getConversation` returns undefined — `hand-back.ts:23` — the `!` non-null assertion crashes with a cryptic error; untested.
- [ ] Add test: `mainAgent` fallback `"Main Agent"` display name — `hand-back.ts:26`.

#### `services/copilot-service.ts`
- [ ] Add tests: `topicBoundaries` validation branches (missing `allowed`, empty `boundaryMessage`, non-string list elements) — `copilot-service.ts:162-178`.
- [ ] Add test: one-line agent-config block without `\n` before closing backtick doesn't match regex — `copilot-service.ts:115`.

### web-client (`packages/web-client`)

#### `lib/api.ts`
- [ ] Add test: `onTitle` SSE event parsed and fires callback — `api.ts` — backend emits `title`; frontend handler updates sidebar title; zero assertion coverage.
- [ ] Add test: `onDelegationStart` / `onDelegationEnd` SSE events parsed and fire callbacks — `api.ts` — delegation banner and specialist bubble driven by these; silent break risk is high.
- [ ] Add test: Bearer-token propagation — seed `localStorage.auth_token`, call any authenticated endpoint, assert `Authorization: Bearer …` header is present in the `fetch` call — `api.ts:authHeaders()`.
- [ ] Add tests: `getStoredToken`, `setStoredToken`, `clearStoredToken` — `api.ts` — auth helpers have no direct coverage.
- [ ] Add test: `onAssignment` SSE line parsed in `api.ts` (not just hook-level mock) — `api.ts` — wire-level parsing is untested.
- [ ] Add test: `onSummary` / `onDebugSummary` SSE events parsed — `api.ts`.
- [ ] Add test: multi-chunk SSE buffering (partial line carried across two chunks) — `api.ts:150-152` — every existing test enqueues full payload in one chunk; TCP-chunked streams are untested.
- [ ] Add tests: `listConversations()` success and error paths — `api.ts`.
- [ ] Add tests: `deleteConversation()` success and 4xx paths — `api.ts`.

#### `hooks/use-chat.ts`
- [ ] Add test: `selectConversation` — loads detail, maps messages, sets summary — `use-chat.ts` — most common post-login user action is untested.
- [ ] Add test: `deleteConversation` with active conversation removed (clears `conversationId` + `messages`) — `use-chat.ts`.
- [ ] Add test: `switchAgent` with delete-if-empty optimization (calls `apiDeleteConversation` when no user messages exist) — `use-chat.ts`.
- [ ] Add test: SSE `event: error` path into `onError` removes placeholder and sets `state.error` — `use-chat.ts`.
- [ ] Add test: `resolveAgentId` prefers `"router"` agent; honors `lastAgentId` only when non-router and in list — `use-chat.ts:45-64`.
- [ ] Add tests: `setSummaryEnabled` and `refreshSummary` make correct network calls — `use-chat.ts`.

#### `lib/agents-api.ts`
- [ ] Add tests: `updateAgent` and `fetchAgents` error paths (4xx, 5xx) — `agents-api.ts`.

#### `lib/copilot-api.ts`
- [ ] Add tests: `agent_created`, `agent_updated`, `delta`, `error` SSE event types — `copilot-api.ts:62-78` — only `done` is exercised; `agent_created` drives the drawer-open side-effect.

---

## 4. Untested Modules

### agent-service

- [ ] Write integration test for Express app composition in `index.ts` — `packages/agent-service/src/index.ts` — CORS configuration, middleware ordering, and router mounting are unverified; a broken `index.ts` passes every current test because route-level tests each build their own Express instance.

### web-client — Hooks

- [ ] Write tests for `use-copilot.ts` mode detection, streaming, and state transitions — `packages/web-client/src/hooks/use-copilot.ts` — the `/^(edit|update|modify)\s+(.+)/i` regex is a silent-failure point; a miss routes users into create-mode and generates a duplicate agent.
- [ ] Write tests for `use-agents.ts` CRUD state, error propagation, and re-fetch after mutation — `packages/web-client/src/hooks/use-agents.ts` — every agent-drawer action depends on this hook; no test asserts the error state surfaces.

### web-client — Contexts

- [ ] Write tests for `AuthContext.tsx` JWT decode, expiry check, login/logout, token persistence — `packages/web-client/src/contexts/AuthContext.tsx` — the `exp * 1000 > Date.now()` comparison is the highest-priority untested line in the frontend; a one-character flip or missing field silently locks all users out.

### web-client — Components (prioritized subset)

- [ ] Write tests for `AuthPage.tsx` form submission, mode toggle, and error-state clearing — `packages/web-client/src/components/AuthPage.tsx` — entry point for all unauthenticated users; error state persisting across login/signup mode toggle is a known regression risk.
- [ ] Write tests for `agent-form.tsx` tool-checkbox state and dirty-form detection — `packages/web-client/src/components/agent-form.tsx` — checkbox state bug could silently remove tools from an agent on save.
- [ ] Write tests for `chat-container.tsx` retry-button behavior — `packages/web-client/src/components/chat-container.tsx` — retry calls `startNewChat()` and discards the current conversation; incorrect routing is irreversible for the user.
- [ ] Write tests for `chat-input.tsx` extension allowlist and 10MB size limit — `packages/web-client/src/components/chat-input.tsx` — client-side enforcement; regression could allow disallowed file types.
- [ ] Write tests for `delegation-banner.tsx` rendering from `delegationMeta` shape — `packages/web-client/src/components/delegation-banner.tsx` — schema drift from SSE event (W2) would silently blank delegation banners.
- [ ] Write tests for remaining components (`agent-drawer.tsx`, `sidebar.tsx`, `copilot-panel.tsx`, `copilot-chat.tsx`, `summary-panel.tsx`, `summary-toggle.tsx`, `debug-panel.tsx`, `message-bubble.tsx`, `message-list.tsx`) — `packages/web-client/src/components/` — all 21 components have zero tests; visual regressions, state bugs, and schema-drift failures are invisible.
